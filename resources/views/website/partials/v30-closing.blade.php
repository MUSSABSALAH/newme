<section class="closing">
  <div class="in">
    <h2>{{ $cms->text('homepage', 'closing_h2') }}</h2>
    <p class="tag">{{ $cms->text('homepage', 'closing_tag') }}</p>
    <p class="k">{{ $cms->text('homepage', 'closing_k') }}</p>
    <p style="margin-top:16px"><a href="{{ route('website.subscribe') }}" class="btn white sm">{{ $cms->text('homepage', 'closing_btn') }}</a></p>
    <p class="fine">{{ $cms->text('homepage', 'closing_fine') }}</p>
  </div>
</section>
