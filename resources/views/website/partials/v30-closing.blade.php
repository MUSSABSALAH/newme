<section class="closing">
  <div class="in">
    <h2>{{ __('website.site.closing.h2') }}</h2>
    <p class="tag">{{ __('website.site.closing.tag') }}</p>
    <p class="k">{{ __('website.site.closing.k') }}</p>
    <p style="margin-top:16px"><a href="{{ route('website.subscribe') }}" class="btn white sm">{{ __('website.site.closing.btn') }}</a></p>
    <p class="fine">{{ __('website.site.closing.fine') }}</p>
  </div>
</section>
